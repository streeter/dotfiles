#!/bin/bash


: ${UNAME=$(uname)}


alias xmltidy='xmllint --format'

if [ "$UNAME" = Darwin ]; then
    alias ls='ls -GF'
    alias ll='ls -lk'

    alias gvim='open -a /Applications/MacVim.app'

    alias git=hub

    alias subl='subl -n'
    alias sub=subl
    alias mate=subl
fi
