command script import /Users/streeter/dev/lldb-mcslldbtoolkit/mcslldb_main.py
command script import /usr/local/opt/chisel/libexec/fblldb.py

#command ivars _ivarDescription
#command meth _methodDescription

# List more stuff when printing the debug deescription
set set target.max-string-summary-length 50000
# Then we can do stuff like
# p [[NSNotificationCenter defaultCenter] debugDescription]
# expr (void) NSLog([[NSNotificationCenter defaultCenter] debugDescription])

# Fix missing UIKit symbols
display @import UIKit
target stop-hook add -o "target stop-hook disable"
